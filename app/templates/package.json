{
  "name": "<%= appname %>",
  "version": "0.0.0",
  "description": "",
  "keywords": [
  ],
  "main": "src/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {<% if (includeAngular) { %>
    "angular": "^1.3.16"<% } %>
  }
}
